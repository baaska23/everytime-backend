package timetable

//   Timetable
//   - GET /timetable — get own schedule
//   - POST /timetable/slots — add course slot
//   - DELETE /timetable/slots/:id — remove course slot

type Repository interface {
	
}