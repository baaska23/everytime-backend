package marketplace
//   Marketplace
//   - GET /marketplace/listings — list listings
//   - POST /marketplace/listings — create listing
//   - GET /marketplace/listings/:id — get listing detail
//   - PUT /marketplace/listings/:id — update own listing
//   - DELETE /marketplace/listings/:id — delete own listing
//   - POST /marketplace/listings/:id/interest — express interest (triggers contact reveal)

type Repository interface {
	
}