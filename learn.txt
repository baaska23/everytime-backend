  When an admin views the moderation queue, the flow is:                                                                                                                                                               
  1. Fetch all PostReport rows with status = 'pending'
  2. Use the PostID to fetch the Post separately — or join in a raw query                                                                                                                                              
            
  You're never doing:                                                                                                                                                                                                  
  db.Preload("Post").Find(&reports) // never needed
  report.Post.Title                 // never accessed this way                                                                                                                                                         
                                                                                                                                                                                                                       
  The association field (Post *Post) only exists to enable that Preload pattern in Go. If you don't need it, don't add it.   

    Option B — FeedSource interface (recommended)

  Define a small interface that each feature implements:

  // in internal/board/ or internal/shared/
  type FeedSource interface {
      FeedItems(ctx context.Context) ([]FeedItem, error)
  }

  Then each module (board, marketplace, lostfound) implements FeedSource on its own repo/service. FeedService just iterates over a []FeedSource, merges results, interleaves ads, sorts by time, and returns.

  type FeedService struct {
      sources []FeedSource
      adRepo  ads.AdRepository
  }

  Why this is better:
  - Adding a new feed source = implement the interface, register it in server.go. Zero changes to FeedService.
  - Each module owns its own query logic and filtering.
  - No circular imports — the interface lives in shared or in board since that's where FeedItem already lives.

  Where to put the shared types

  Move FeedItem and FeedSource to internal/shared/feed/ (just the types, not the service). That way board, marketplace, and lostfound can all import the shared type without importing each other. The FeedService stays wherever makes sense — board or server.

  Summary

  ┌─────────────────────────┬────────────────────┬─────────────────────────────────────────┐
  │        Approach         │        Pros        │                  Cons                   │
  ├─────────────────────────┼────────────────────┼─────────────────────────────────────────┤
  │ Separate internal/feed/ │ Clear separation   │ Dependency magnet, no real domain logic │
  ├─────────────────────────┼────────────────────┼─────────────────────────────────────────┤
  │ Widen FeedService deps  │ Simple, works now  │ Grows linearly with each new source     │
  ├─────────────────────────┼────────────────────┼─────────────────────────────────────────┤
  │ FeedSource interface    │ Open/closed, clean │ Slightly more abstraction upfront       │
  └─────────────────────────┴────────────────────┴─────────────────────────────────────────┘

  I'd go with Option B + moving FeedItem/FeedSource to internal/shared/feed/. Want me to implement it?


  Yes, make it shared at the product level, but not inside the low-level shared utilities folder.

Best placement:

Create a new vertical slice like internal/feed for cross-domain aggregation.
Keep domain logic in each module:
board source in board
marketplace source in marketplace
courses source in courses
Wire endpoint in routes.go, similar to how board feed is currently wired via service.go.
Recommended architecture:

Domain source interfaces
Feed module depends on small read-only interfaces from board/marketplace/courses.
Example responsibilities: list recent posts, active listings, recent course reviews/ratings.
Feed service (in internal/feed)
Fetch from 3 sources
Normalize into one FeedItem model
Rank/merge/sort
Inject banners/ads
Apply pagination cursor
Feed handler
Endpoint like GET /feed
Optional filters: university, type, page/cursor
Should it go in internal/shared?

No for business feed orchestration.
Yes only for generic helpers used by feed (pagination utils, response envelope, time helpers), which can live in shared.
Why this is future-proof:

You avoid coupling board with marketplace/courses.
You can evolve ranking independently.
You can add new feed sources later (lostfound, admin announcements) without touching domain repositories heavily.
If you want, I can sketch the exact interface contracts for a new internal/feed slice next (only interfaces and DTOs, no implementation).

send POST
get mac from mobile
find subId
login and return no content, bad response, success

#GORM cheat sheet

.First(&x) select ... order by id limit 1
.Last(&x) select ... order by id desc limit 1
.Take(&x) select ... limit 1
.Find(&x) select ...
.Count(&n) select count(*)

.Create(&x) insert into 
.Save(&x) update ... set (all columns)
.Updates(map) update ... set(non-zero columns)
.Update('col', val) update ... set col=val (single column)
.Delete(&x) update ... set deleted_at=now (soft delete)

.Where("id = ?", 5)
.Not("col = ?", v)
.Or("col = ?", v)
.Order("col desc")
.Limit(n)
.Group("col")


BoringResponse